type Query {
  seeAllPost(year: Int!, month: Int!, isBest: Boolean): [Post!]!
}

type Mutation {
  seeAllPostPaging(
    year: Int!
    month: Int!
    page: Int!
    isBest: Boolean
  ): [Post!]!
}
